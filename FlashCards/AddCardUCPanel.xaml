<UserControl x:Class="FlashCards.AddCardUCPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FlashCards"
             mc:Ignorable="d" 
             MinHeight="250"
             MinWidth="150"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             d:DesignHeight="450" d:DesignWidth="400">

    <UserControl.Resources>
        <!-- Watermark for TextBox -->
        <Style x:Key="WatermarkTextBox" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost"/>
                            <TextBlock x:Name="PART_Watermark"
                                       Text="{TemplateBinding Tag}"
                                       Foreground="Gray"
                                       Margin="4,2,0,0"
                                       IsHitTestVisible="False"
                                       Opacity="0.6"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8 4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="10"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Viewbox Stretch="Uniform">
        <Border BorderBrush="Gray" BorderThickness="0.5" CornerRadius="10"
                Padding="10" Background="White">
            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!--Add pict butt-->
                <Button Grid.Row="0" Grid.Column="0" Content="Add Picture"
                        Width="120" Height="120" Margin="0,0,0,10"
                        Style="{StaticResource RoundedButton}"/>

                <!--Pict-->
                <Image Grid.Row="0" Grid.Column="0"
                       Margin="0,40,0,10"
                       Height="150"
                       Stretch="Uniform"
                       Visibility="Collapsed"
                       x:Name="CardImage"/>

                <!--Word-->
                <TextBox x:Name="WordTextBox"                
                         Grid.Row="1"
                         Grid.Column="0"
                         Margin="0,10,0,10"
                         Height="30"
                         VerticalContentAlignment="Center"
                         Style="{StaticResource WatermarkTextBox}"
                         Tag="Write a word"/>

                <!--Senntence-->
                <TextBox x:Name="SentenceTextBox"          
                         Grid.Row="2"
                         Grid.Column="0"
                         Margin="0,0,0,10"
                         Height="60"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         Style="{StaticResource WatermarkTextBox}"
                         Tag="Write a sentence"/>

                <!--Save/Cancel -->
                <StackPanel Grid.Row="3" Grid.Column="0"
                            Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Bottom"
                            Margin="0,10,0,0">
                    <Button Content="Save" Width="80" Height="30" Margin="5"
                            Style="{StaticResource RoundedButton}"/>
                    <Button Content="Cancel" Width="80" Height="30" Margin="5"
                            Style="{StaticResource RoundedButton}"
                            Click="CancelButtonClick"/>
                </StackPanel>

                <!--Flip -->
                <Button Grid.Row="1" Grid.RowSpan="2" Grid.Column="1"
                        Content="Flip"
                        Width="30" Height="60"
                        VerticalAlignment="Center"
                        Margin="30,0,-10,0"
                        Style="{StaticResource RoundedButton}"/>

            </Grid>
        </Border>
    </Viewbox>

</UserControl>
